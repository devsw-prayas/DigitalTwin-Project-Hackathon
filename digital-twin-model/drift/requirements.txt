# DRIFT — Requirements
# Install with: pip install -r requirements.txt
#
# Core
torch>=2.1.0
numpy>=1.24.0
scipy>=1.10.0

# Flash Attention (optional but strongly recommended for GPU)
# Requires CUDA 11.6+ and a compatible GPU
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.3.0

# API backend
fastapi>=0.104.0
uvicorn>=0.24.0
sqlmodel>=0.0.14
pydantic>=2.0.0

# Serving (production)
# torchserve  -- install via: pip install torchserve torch-model-archiver
# onnxruntime>=1.16.0  -- for CPU inference fallback

# Monitoring / logging
tqdm>=4.66.0

# Dev / eval
matplotlib>=3.7.0
